name: MLOps Pipeline

on:
  push:
    branches: [ master ]
  pull_request:

jobs:
  build-and-train:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install dvc[s3] mlflow scikit-learn joblib
        pip install 'pathspec<0.12.0'

    - name: Pull DVC data
      run: dvc pull

    - name: Run DVC pipeline
      run: dvc repro

    # Ajoute ici d'autres étapes si besoin (upload artefacts, etc.)
